import urlsJson from 'constants/json/urls.json';
import reduxJson from 'constants/json/redux.json';
import interfaceJson from 'constants/json/interface.json';

const Constants = {
  urls: urlsJson,
  redux: reduxJson,
  interface: interfaceJson,
};

export default Constants;
